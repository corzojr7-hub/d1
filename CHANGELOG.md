# CHANGELOG - Sistema de Control Operativo de Tienda

## 2026-06-02

- Creado contexto inicial del proyecto.
- Creado PRD inicial del MVP.
- Creado roadmap inicial por fases.
- Registradas decisiones iniciales.
- Creada primera tarea NextAx para revision y fortalecimiento documental.
- Revisada y fortalecida la documentacion (MVP-001): agregados arquitectura de alto nivel, riesgos y mitigaciones, y criterios de exito en PRD.md.
- Registrada decision DEC-009 en DECISIONS.md.
- Aprobada la tarea MVP-001 por cumplir criterios de aceptacion y no modificar archivos prohibidos.
- Generada la tarea MVP-002 para definir arquitectura inicial concreta antes de iniciar codigo.
- Creado ARCHITECTURE.md para MVP-002 con entidades conceptuales, roles, permisos, autenticacion, almacenamiento, codigos de barras y estructura tecnica esperada.
- Actualizado ROADMAP.md para referenciar ARCHITECTURE.md dentro de la Fase 1.
- Aprobada la tarea MVP-002 por cumplir criterios de aceptacion y mantener el alcance sin codigo.
- Generada la tarea MVP-003 para inicializar la base tecnica Next.js sin implementar modulos de negocio.

## 2026-06-03

- Actualizada la estrategia multi-IA por agotamiento cercano de OpenCode Go.
- Registrada DEC-010: politica inteligente de seleccion de modelos segun costo, calidad, riesgo y tipo de tarea.
- Actualizado PROJECT_CONTEXT.md con politica de seleccion, roles de modelos, alternativas, tareas pequenas y reglas de escalamiento.
- Actualizado ROADMAP.md con estrategia flexible de ejecucion multi-IA y obligacion de justificar el modelo en cada NEXT_ACTION.
- Regenerada NextAx para que MVP-003 incluya "Justificacion del modelo seleccionado", alternativa gratuita y escalamiento explicito.
- MVP-003: Inicializada la base tecnica Next.js con TypeScript y Tailwind CSS. Creada pantalla inicial responsive tipo shell operativa con dos accesos visuales deshabilitados para futuros modulos. Actualizados README.md y configuracion de lint. Generado por DeepSeek V4 Flash Free.
- Revisada MVP-003: lint y build pasan, pero se solicita correccion menor de textos visibles/encoding antes de aprobar la tarea.
- MVP-003-CORR-001: Corregidos textos visibles no ASCII en page.tsx (`revision`, `(c)` y `-`).
- Aprobada MVP-003 completa tras correccion menor: base Next.js, TypeScript y Tailwind lista; lint y build pasan.
- Actualizado ROADMAP.md: Fase 2 completada y Fase 3 iniciada.
- Generada MVP-004 para preparar cliente Supabase y variables de entorno sin implementar autenticacion, tablas ni migraciones.
- MVP-004: Instalado @supabase/supabase-js. Creados .env.example y src/lib/supabase/client.ts con helper minimo. Actualizados README.md y CHANGELOG.md.
- Aprobada MVP-004: lint y build pasan; no se creo .env.local, carpeta supabase ni migraciones.
- Generada MVP-005 para crear una propuesta SQL inicial de esquema Supabase, sin aplicarla a una base real.
- MVP-005: Creada propuesta SQL inicial en database/schema.sql con tablas profiles, products, instructions y waste_records. Creado database/README.md con estado y pendientes. Sin RLS, seeds, Storage ni migraciones aplicables.
- Aprobada MVP-005: el esquema propuesto cumple alcance, no contiene RLS, policies, seeds, credenciales, carpeta supabase ni migraciones aplicables.
- Generada MVP-006 para revision QA tecnica del esquema SQL antes de correcciones, RLS o aplicacion en Supabase real.
- MVP-006: QA tecnico completado. Revisado schema.sql contra PRD.md y ARCHITECTURE.md. Creado database/SCHEMA_REVIEW.md con hallazgos clasificados (0 bloqueantes, 3 importantes, 3 menores, 7 observaciones). No se modifico schema.sql ni se crearon migraciones, RLS, policies, seeds ni carpeta supabase. Recomendacion: aprobar esquema para correccion antes de habilitar RLS.
- Aprobada MVP-006: revision QA aceptada, con 0 bloqueantes y correcciones tecnicas pendientes.
- Registrada DEC-011 y ajustado PRD.md: observacion de merma requerida; foto de evidencia condicional cuando aplique.
- Generada MVP-007 para corregir schema.sql segun hallazgos QA, sin RLS, policies, seeds ni Supabase real.
- MVP-007: Corregido schema.sql segun SCHEMA_REVIEW.md. Agregados: funcion set_updated_at(), triggers before update en las 4 tablas, constraint chk_product_not_found_coherence en waste_records, unique en profiles.email, not null en waste_records.observation. Documentado que perfiles historicos deben inactivarse, no eliminarse. evidence_path sigue nullable. Actualizados database/README.md y CHANGELOG.md.
- Aprobada MVP-007: correcciones SQL verificadas; no se agregaron RLS, policies, seeds, migraciones, carpeta supabase ni nuevas tablas.
- Generada MVP-008 para definir plan documental de seguridad y RLS antes de escribir policies SQL.
- MVP-008: Creado database/RLS_PLAN.md con principios de seguridad, matriz de permisos por rol y reglas RLS futuras en lenguaje natural, sin SQL ejecutable ni cambios en schema.sql.
- Aprobada MVP-008: plan RLS documental verificado sin SQL ejecutable, sin cambios a schema.sql y sin carpeta supabase.
- Registrada DEC-012: no se modela encargatura tecnica en MVP; acciones criticas reservadas a supervisor; productos internos por carga tecnica/manual.
- Actualizado database/RLS_PLAN.md para eliminar ambiguedad de encargatura y administracion de productos antes de policies SQL.
- Generada MVP-009 para crear propuesta SQL de RLS/policies en archivo documental, sin aplicarla a Supabase real.
- MVP-009: Creado database/rls_policies.sql con funciones helper (active_profile, active_role), habilitacion RLS y policies para profiles, products, instructions y waste_records. Respeta DEC-012: sin encargatura, acciones criticas solo para supervisor, productos bloqueados desde app. Documentadas limitaciones de RLS por columnas. Sin seeds, migraciones ni cambios a schema.sql. Actualizados database/README.md y CHANGELOG.md.
- Revisada MVP-009: requiere correccion tecnica antes de aprobacion por uso invalido de alias `new` en policies RLS y necesidad de endurecer helpers SQL.
- MVP-009-CORR-001: Corregido rls_policies.sql: reemplazados `new.status` -> `status`, `new.review_status` -> `review_status`. Endurecidos helpers con `set search_path = public` y referencias calificadas (`p.role`, `p.user_id`, `p.status`). Sin cambios en permisos aprobados ni en schema.sql.
- Revisada MVP-009-CORR-001: requiere segunda correccion tecnica por helpers en esquema `auth` y riesgo de recursion RLS al consultar `profiles` dentro de policies de `profiles`.
- MVP-009-CORR-002: Migrados helpers de `auth` a `public`. Creados `public.current_app_profile_id()`, `public.current_app_role()`, `public.is_active_app_user()`. Eliminada subconsulta directa a `public.profiles` en `profiles_select_active`. Reemplazadas todas las validaciones de perfil activo por `public.is_active_app_user()`. Reemplazado `auth.active_role()` por `public.current_app_role()`. Sin cambios en permisos aprobados ni en schema.sql.
- Aprobada MVP-009 completa tras correcciones: propuesta RLS verificada sin `new.`, sin helpers propios en `auth`, sin recursion directa en `profiles`, sin seeds, sin migraciones y sin Supabase real.
- Generada MVP-010 para QA tecnico de `database/rls_policies.sql` antes de cualquier aplicacion o migracion.
- MVP-010: QA tecnico de seguridad/RLS completado. Revisado rls_policies.sql contra RLS_PLAN.md, schema.sql, DECISIONS.md, PRD.md y ARCHITECTURE.md. Creado database/RLS_REVIEW.md con hallazgos clasificados (0 bloqueantes, 3 importantes, 3 menores, 7 observaciones). No se modifico rls_policies.sql ni schema.sql. No se crearon migraciones, carpeta supabase ni archivos SQL nuevos. Recomendacion: aprobar propuesta RLS para correccion antes de aplicar en Supabase real.
- Aprobada MVP-010: revision QA aceptada, con 0 bloqueantes y correcciones tecnicas pendientes.
- Actualizado database/RLS_PLAN.md para alinear completamente la matriz de permisos con DEC-012: segundo_al_mando no anula ni cierra estados criticos; productos internos no se administran desde app.
- Generada MVP-011 para corregir rls_policies.sql segun QA, sin aplicar SQL en Supabase real.
- MVP-011: Corregido rls_policies.sql por hallazgos de MVP-010. Agregadas validaciones de trazabilidad: inserts exigen `created_by = current_app_profile_id()` y estado inicial (`status = 'pendiente'`, `review_status = 'pendiente_revision'`); updates exigen `updated_by = current_app_profile_id()`. Reemplazado `auth.role() = 'authenticated'` por `auth.uid() is not null`. Documentado requisito de BYPASSRLS en helpers security definer. Sin cambios en schema.sql, RLS_PLAN.md, migraciones ni carpeta supabase.
- Aprobada MVP-011: rls_policies.sql verificado sin `auth.role()`, sin `new.`, sin helpers antiguos, sin seeds, sin migraciones y sin Supabase real.
- Generada MVP-012 para documentar paquete y checklist de aplicacion manual de SQL en Supabase, sin ejecutar nada.
- MVP-012: Creado database/APPLY_SUPABASE_CHECKLIST.md con orden recomendado, precondiciones, checklist de seguridad, verificaciones por rol, riesgos y procedimiento de cierre para futura aplicacion manual de schema.sql y rls_policies.sql en Supabase real. Menciona BYPASSRLS para helpers y DEC-012. Actualizada referencia en database/README.md. Sin ejecucion de SQL, sin cambios en scripts SQL, sin carpeta supabase, sin migraciones y sin modificacion de frontend.
- Revisada MVP-012: requiere correccion menor de encoding en APPLY_SUPABASE_CHECKLIST.md antes de aprobacion.
- MVP-012-CORR-001: Reemplazados caracteres no ASCII y texto corrupto en database/APPLY_SUPABASE_CHECKLIST.md. Documento ahora es 100% ASCII. Sin cambios en schema.sql, rls_policies.sql, frontend ni migraciones.
- Aprobada MVP-012 completa tras correccion menor de encoding.
- Generada MVP-013 para crear tipos TypeScript y catalogos de dominio basados en PRD/schema, sin conectar Supabase real.
- MVP-013: Creados src/lib/domain/types.ts (interfaces Profile, Product, Instruction, WasteRecord), catalogs.ts (catalogos con value/label para roles, estados, prioridades y motivos) e index.ts (reexport). Sin conexion a Supabase ni cambios en app, supabase, database o SQL.
- Aprobada MVP-013: tipos y catalogos verificados; lint y build pasan; sin cambios en src/app, database, SQL ni Supabase real.
- Generada MVP-014 para crear validaciones puras de dominio en TypeScript, sin formularios ni dependencias nuevas.
- MVP-014: Creado src/lib/domain/validators.ts con ValidationIssue/ValidationResult, helpers (isNonEmptyText, isPositiveNumber, isCatalogValue) y validadores para instruccion, merma y producto. Actualizado index.ts. Sin dependencias nuevas, sin Supabase, sin formularios ni cambios en app/database.
- Aprobada MVP-014: validadores verificados; lint y build pasan; sin dependencias nuevas ni cambios en app, database o Supabase.
- Generada MVP-015 para agregar pruebas unitarias ligeras de validadores usando node:test, sin instalar dependencias.
- Revisada MVP-015: lint, build y test pasan, pero requiere correccion por modificar archivos prohibidos (`tsconfig.json`, `src/lib/domain/validators.ts`) y cambiar el formato de test solicitado.
- MVP-015: Creado tests/domain/validators.test.mjs con pruebas node:test + assert/strict. Agregado script "test" en package.json. Validacion de instrucciones, merma y productos con casos validos e invalidos. Sin dependencias nuevas, sin Supabase, sin cambios en app/database.
- MVP-015-CORR-001: Revertidos cambios prohibidos en validators.ts (import catalogs sin .ts) y tsconfig.json (allowImportingTsExtensions). Creado tests/domain/resolver.mjs para resolver imports extensionless .ts via loader hook nativo. Test file cambiado a .mjs con 18 tests. Eliminado tests/domain/validators.test.ts. Sin type:module en package.json. Sin dependencias nuevas.
- Aprobada MVP-015 tras correccion: lint, build y test pasan; 18 tests verdes; sin dependencias nuevas, sin cambios en database, src/app ni Supabase real.
- Generada MVP-016 para crear formulario UI local de Instrucciones Operativas con validacion de dominio, sin persistencia ni Supabase.
- MVP-016: Creados src/app/instructions/new/page.tsx e InstructionForm.tsx. Formulario con campos responsable, instruccion, prioridad (select desde INSTRUCTION_PRIORITIES), estado (select desde INSTRUCTION_STATUSES) y observaciones. Validacion via validateInstructionInput. Errores inline por campo. Mensaje local de exito sin persistencia. Boton Limpiar. Tarjeta IO en pagina principal ahora enlaza a /instructions/new. Sin conexion Supabase, sin API routes, sin server actions, sin dependencias nuevas. lint, build y test pasan.
- Aprobada MVP-016: verificada ruta /instructions/new, validacion vacia, exito local, lint/build/test y respuesta HTTP local. Sin Supabase, persistencia, API routes ni cambios en database.
- Generada MVP-017 para crear formulario UI local de Registro de Merma con validacion de dominio, sin escaner ni persistencia.
- MVP-017: Creados src/app/waste/new/page.tsx y WasteForm.tsx. Formulario con campos: codigo de barras, checkbox producto no encontrado, ID producto condicional, cantidad, unidad, motivo (select via WASTE_REASONS), estado revision (select via WASTE_REVIEW_STATUSES), responsable, area, observacion y nota informativa de foto pendiente. Validacion via validateWasteRecordInput. Errores inline. Exito local sin persistencia. Tarjeta MR en pagina principal enlaza a /waste/new. Sin escaner, sin Supabase, sin API routes, sin server actions, sin dependencias nuevas. lint, build, test pasan.
- Aprobada MVP-017: verificada ruta /waste/new, enlace desde inicio, validacion vacia, logica de exito local, lint/build/test y ausencia de cambios en database o Supabase real.
- Generada MVP-018 para crear paginas indice placeholder de Instrucciones y Merma, con navegacion operativa basica y sin persistencia.
- MVP-018: Creados src/app/instructions/page.tsx y src/app/waste/page.tsx como indices placeholder con titulo, descripcion de estado pendiente, enlace a formulario existente y vuelta a inicio. Actualizada pagina principal para enlazar a /instructions y /waste en vez de directo a formularios. Navegacion: /instructions enlaza a /instructions/new; /waste enlaza a /waste/new. Sin Supabase, sin persistencia, sin CRUD. lint, build, test pasan.
- Revisada MVP-018: lint, build y test pasan, pero requiere correccion menor de texto visible por uso de `&larr;` en enlaces de vuelta, que renderiza una flecha no ASCII.
- MVP-018-CORR-001: Reemplazado `&larr; Volver al inicio` por `Volver al inicio` en src/app/instructions/page.tsx y src/app/waste/page.tsx para eliminar entidad HTML no ASCII. Sin cambios en estructura, navegacion, database ni tests.
- Aprobada MVP-018 completa tras correccion: rutas /instructions y /waste verificadas, navegacion a formularios correcta, textos visibles ASCII, lint/build/test pasan y sin cambios en database ni Supabase real.
- Generada MVP-019 para ajustar navegacion de retorno en formularios hacia los indices de modulo y eliminar flechas no ASCII restantes.
- MVP-019: Ajustados enlaces de retorno en formularios - /instructions/new ahora apunta a /instructions con texto "Volver a instrucciones"; /waste/new ahora apunta a /waste con texto "Volver a merma". Eliminadas flechas &larr; no ASCII. Sin cambios en database, SQL ni dependencias.
- Aprobada MVP-019: verificados enlaces de retorno en navegador local, lint/build/test pasan, sin cambios en database, Supabase, dependencias ni componentes de formulario.
- Generada MVP-020 para mostrar resumen local del registro validado en formularios, sin persistencia ni backend.
- MVP-020: Agregado estado lastValid y bloque de resumen local en InstructionForm.tsx y WasteForm.tsx. En instrucciones muestra responsable, prioridad, estado, instruccion y observaciones. En merma muestra codigo de barras, producto (o "No encontrado en base"), cantidad/unidad, motivo, estado revision, responsable, area y observacion. Resumen desaparece al modificar cualquier campo o pulsar Limpiar. Sin Supabase, persistencia, localStorage ni dependencias. lint, build, test pasan.
- Aprobada MVP-020: revision de codigo, lint/build/test y verificaciones de alcance correctas; sin cambios en database, Supabase, rutas, dependencias ni validadores. Prueba automatizada de escritura en navegador limitada por herramienta local, sin hallazgos bloqueantes.
- Generada MVP-021 para alinear los estados iniciales visibles en formularios con las reglas futuras de insert en RLS: pendiente y pendiente_revision.
- MVP-021: Eliminados selects editables de status en InstructionForm.tsx y review_status en WasteForm.tsx. Reemplazados por texto no editable "Estado inicial: Pendiente" y "Estado inicial: Pendiente de revision". Los valores fijos se mantienen en estado (form.status, form.review_status) y se pasan al validador y al resumen local. Sin cambios en validadores, catalogos, tipos, SQL ni RLS. lint, build, test pasan.
- Aprobada MVP-021: verificados estados iniciales fijos por codigo y navegador, ausencia de selectores editables de estado, lint/build/test y SQL intactos.
- Generada MVP-022 para crear constructores puros y pruebas de payloads de insercion, sin conectar Supabase.
- MVP-022: Creados src/lib/domain/payloads.ts con InstructionCreateData, WasteRecordCreateData, InstructionInsertPayload, WasteRecordInsertPayload, buildInstructionInsertPayload y buildWasteRecordInsertPayload. Constructores fuerzan status=pendiente, review_status=pendiente_revision, convierten observaciones vacias a null, anulan product_id si product_not_found=true. Reexportados desde index.ts. Creado tests/domain/payloads.test.mjs con 12 tests. npm test ejecuta ambos archivos (30/30 tests). Sin Supabase, frontend ni dependencias. lint, build, test pasan.
- Revisada MVP-022: lint/build y 30 tests pasan, pero requiere correccion importante porque el constructor de merma puede producir `product_id = null` con `product_not_found = false`, payload rechazado por `chk_product_not_found_coherence`.
- MVP-022-CORR-001: Corregido buildWasteRecordInsertPayload para garantizar coherencia product_id/product_not_found: si product_id esta ausente o vacio, fuerza productNotFound=true y product_id=null; si product_id presente y flag distinto de true, usa false. Agregada prueba "force product_not_found true when product_id missing even if flag is false". Actualizada prueba de opcionales ausentes para afirmar product_not_found=true. 31/31 tests pasan.
- Aprobada MVP-022 completa tras correccion: constructores de payload alineados con estados iniciales, created_by y constraint de coherencia; lint/build y 31 tests pasan; sin Supabase real ni cambios en SQL.
- Generada MVP-023 para validar formato UUID de product_id antes de construir futuros inserts de merma.
- MVP-023: Agregado helper isUuid y validacion en validateWasteRecordInput. Nuevo issue product_id si ID presente no es UUID valido. Agregados 9 tests (6 isUuid + 3 validacion UUID). 40/40 tests pasan. Sin cambios en payloads, frontend, schema, SQL ni RLS. lint, build, test pasan.
- Aprobada MVP-023: helper UUID, validacion de product_id y flujo sin producto verificados; lint/build y 40 tests pasan; payloads, frontend, SQL y Supabase intactos.
- Generada MVP-024 para unificar validacion y construccion de payload en preparadores puros de submission, sin conectar Supabase.
- MVP-024: Creados src/lib/domain/submissions.ts con SubmissionPreparationResult<T>, prepareInstructionSubmission y prepareWasteRecordSubmission. Preparadores validan datos (via validator), validan perfil actual (via isUuid), acumulan issues de ambos; si hay fallos devuelven ok=false con issues sin construir payload; si todo ok devuelven ok=true con payload del constructor. Reexportados desde index.ts. Creado tests/domain/submissions.test.mjs con 14 tests (6 instruccion + 8 merma). Script test actualizado para 3 archivos. 54/54 tests pasan. Sin cambios en validators, payloads, frontend, database, SQL, RLS ni Supabase. lint, build, test pasan.
- Aprobada MVP-024: preparadores discriminados verificados; datos/perfil invalidos no producen payload, issues se acumulan y validos conservan estados/created_by. lint/build y total real de 54 tests pasan; sin Supabase real ni cambios en validators, payloads o SQL.
- Generada MVP-025 para revision QA integral de preparacion de persistencia antes de conectar o aplicar Supabase real.

## 2026-06-04

- MVP-025: QA integral de preparacion para persistencia completado. Revisada coherencia UI-dominio-payload-submission-schema-RLS-checklist. Creado database/PERSISTENCE_READINESS_REVIEW.md con hallazgos clasificados (0 bloqueantes, 4 importantes, 4 menores, 7 observaciones), matriz de preparacion y orden recomendado de tareas. lint, build y 54/54 tests pasan. No se modifico codigo, SQL ni documentos existentes. No se creo supabase/, .env.local ni migraciones. Recomendacion: aprobar con correcciones previas antes de conectar a Supabase real.
- MVP-025-CORR-001: Corregido database/PERSISTENCE_READINESS_REVIEW.md. Eliminados IMP-003, IMP-004 y OBS-006 (falsos frente a rls_policies.sql actual). Recalculados hallazgos importantes: UI-submissions, service layer, autenticacion/currentProfileId y BYPASSRLS sin confirmar. Corregidos conteos de tests a validators 27, payloads 13, submissions 14, total 54. Corregida matriz RLS: protecciones de autoria y estado inicial marcadas como Listo. Reordenadas tareas siguientes sin pedir reimplementar RLS existente. Reemplazado caracter no ASCII en MIN-002. Documento ahora 100% ASCII. lint/build/test pasan.
- Aprobada MVP-025 completa tras correccion: review integral coherente con RLS actual, matriz y orden corregidos, documento ASCII, lint/build y 54 tests pasan; sin cambios en codigo, SQL ni Supabase real.
- Generada MVP-026 para crear una capa de servicios de insercion testeable mediante gateway inyectado, sin conectar Supabase real.
- MVP-026: Creados src/lib/services/operation-inserts.ts con interface OperationInsertGateway, OperationInsertResult (ok/validation issues/error), insertInstruction e insertWasteRecord. Servicios reutilizan prepare*Submission; si la preparacion falla devuelven issues sin llamar gateway; si exito llaman gateway exactamente una vez capturando Error (message) o no-Error (mensaje generico). Creado tests/services/operation-inserts.test.mjs con 11 tests (5 instruccion + 6 merma) usando mocks de gateway y mock.fn. Script test actualizado para 4 archivos. 65/65 tests pasan. Sin cambios en domain/, src/app/, database/, Supabase ni dependencias. lint, build, test pasan.
- Revisada MVP-026: lint/build y 65 tests pasan, pero requiere correccion importante. El contrato publico no coincide con la tarea: `OperationInsertResult` no expone siempre `issues` y `error` con la forma pedida, el orden de parametros de `insertInstruction` e `insertWasteRecord` no coincide con el API solicitado, y las pruebas no verifican el payload exacto enviado al gateway ni la coherencia enviada en merma.
- Generada MVP-026-CORR-001 para alinear contrato de resultados, firmas publicas y pruebas de payload de la capa de servicios antes de aprobar la preparacion de persistencia.
- MVP-026-CORR-001: Verificados `deepEqual` completos para payloads de merma en tests/services/operation-inserts.test.mjs. El contrato de `operation-inserts` queda alineado con la tarea: `OperationInsertResult` fijo con `ok`, `issues` y `error`, firmas publicas `(data, currentProfileId, gateway)`, y tests que validan payload exacto para instruccion y merma. 65/65 tests pasan. Sin cambios en domain/, app/, database/, Supabase ni dependencias. lint, build, test pasan.
- Aprobada MVP-026 completa tras correccion: capa de servicios pura y testeable cerrada, contrato y cobertura alineados, lint/build y 65 tests pasan; sin cambios en SQL, frontend, Supabase real o lockfile.
- Generada MVP-027 para crear un adaptador Supabase testeable que implemente `OperationInsertGateway` sin tocar el dominio ni la UI.
- MVP-027: Creado src/lib/supabase/operation-inserts.ts con factory `createSupabaseGateway(supabase)` que implementa `OperationInsertGateway`. Inserta en `instructions` y `waste_records` usando el cliente inyectado. Convierte error de Supabase a Error con `.message`. Corregido import de payload types desde `../domain/payloads` (no exportados desde services). Creado tests/supabase/operation-inserts.test.mjs con 4 tests (2 instruccion + 2 merma) usando clientes falsos locales con `mock.fn`. Script test actualizado para 5 archivos. 69/69 tests pasan. Sin cambios en services/, domain/, app/, database/, SQL, RLS, Supabase real ni dependencias. lint, build, test pasan.
- Aprobada MVP-027: adaptador Supabase testeable listo, tabla y payload correctos verificados, errores convertidos con mensaje legible, lint/build y 69 tests pasan; sin cambios en UI, dominio, SQL, RLS o Supabase real.
- Generada MVP-028 para crear una capa de aplicacion que componga servicios y gateway sin tocar la UI ni el dominio.
- MVP-028: Creado src/lib/app/operation-submitters.ts con factory `createOperationSubmitter(gateway)` que devuelve `OperationSubmitter` con metodos `submitInstruction` y `submitWasteRecord`. Cada metodo delega en el servicio correspondiente con el gateway inyectado, manteniendo separada la construccion del gateway de la ejecucion del submit. Creado tests/app/operation-submitters.test.mjs con 6 tests (3 instruccion + 3 merma: exito, datos invalidos, error de gateway). Script test actualizado para 6 archivos. 75/75 tests pasan. Sin cambios en services/, domain/, supabase/, app/, database/, SQL, RLS ni dependencias. lint, build, test pasan.
- Aprobada MVP-028: capa de aplicacion ligera lista, delegacion correcta y tests verdes; sin tocar UI, dominio, SQL, RLS ni Supabase real.
- Generada MVP-029 para crear un puente compartido de submit que permita a los formularios usar la capa de aplicacion sin conocer el gateway.
- MVP-029: Creado src/lib/app/operation-submit-flow.ts con `configureSubmitFlow(submitter)`, `submitInstructionFlow(data, profileId)`, `submitWasteRecordFlow(data, profileId)`, `isSubmitFlowConfigured()` y `resetSubmitFlow()`. El puente encapsula un `OperationSubmitter` como singleton configurable en init de la app; los formularios llaman solo `submitInstructionFlow`/`submitWasteRecordFlow` sin conocer gateways ni adaptadores. Creado tests/app/operation-submit-flow.test.mjs con 11 tests (3 config/reset + 4 instruccion + 4 merma: exito, validacion, error persistencia, no configurado). Script test actualizado para 7 archivos. 86/86 tests pasan. Sin cambios en services/, domain/, supabase/, submitters/, database/, SQL, RLS ni dependencias. lint, build, test pasan.
 - Revisada MVP-030: lint/build y 86 tests pasan, pero requiere correccion importante. `noOpSubmitter()` hace que el submit flow marque exito aunque la app no haya sido inicializada, de modo que los formularios pueden mostrar "registrada correctamente" sin persistencia real. El bypass oculta la falta de `configureSubmitFlow(...)` y puede hacer que `isSubmitFlowConfigured()` se vuelva verdadero tras el primer submit sin que exista gateway real.
 - Generada MVP-030-CORR-001 para eliminar el no-op automatico o restablecer una inicializacion explicita segura antes de aprobar la conexion UI-submit flow.
 - MVP-030-CORR-001: Eliminado `noOpSubmitter()` privado de `operation-submit-flow.ts`. `ensureSubmitter()` ahora lanza Error si no se ha llamado `configureSubmitFlow`. Agregado `createDevSubmitter()` como exportacion publica para inicializacion explicita en desarrollo. `layout.tsx` convertido a componente cliente que llama `configureSubmitFlow(createDevSubmitter())` en `useEffect`. Tests de auto-configuracion reemplazados por tests de throw. Agregado test para `createDevSubmitter`. 87/87 tests pasan. lint, build, test pasan.
 - MVP-031: Extraido `DEV_PROFILE_ID` hardcodeado de `InstructionForm.tsx` y `WasteForm.tsx` a `src/lib/app/dev-profile.ts` como fuente unica compartida. Ambos formularios importan `DEV_PROFILE_ID` desde `@/lib/app/dev-profile`. Sin cambios en dominio, SQL, RLS ni Supabase real. 87/87 tests pasan. lint, build, test pasan.
- MVP-032: Creada abstraccion `getCurrentProfileId()` en `src/lib/app/current-profile.ts` que encapsula `DEV_PROFILE_ID`. Los formularios ahora importan `getCurrentProfileId()` en vez de un UUID literal, listos para reemplazar por autenticacion real. Creados `tests/app/current-profile.test.mjs` con 3 tests (no vacio, UUID valido, valor esperado). 90/90 tests pasan. lint, build, test pasan.
- MVP-033: Refactorizado `current-profile.ts` con patron de resolver pluggable. `getCurrentProfileId()` mantiene API estable y auto-configura un resolver de desarrollo por defecto. Agregadas `configureProfileResolver(resolver)`, `resetProfileResolver()`, `isProfileResolverConfigured()`. Tests ampliados de 3 a 10 casos (resolver custom, reset, lazy init, estado de configuracion). Formularios no modificados. 97/97 tests pasan. lint, build, test pasan.
- MVP-034: Creado `src/lib/app/auth-profile.ts` con factory `createAuthProfileResolver()` que produce un `ProfileResolver` listo para inyectar via `configureProfileResolver`. Actualmente lanza "Autenticacion no implementada" como placeholder estructural. Exportado `ProfileResolver` desde `current-profile.ts`. Creados `tests/app/auth-profile.test.mjs` con 4 tests (tipo funcion, throws, compatibilidad con configure, reemplazable por reset). 101/101 tests pasan. lint, build, test pasan.
- MVP-035: Creado `src/lib/app/submission-history.ts` con historial local en memoria compartido entre formularios. Los formularios de instruccion y merma ahora muestran un listado de envios recientes al final del formulario. Creados `tests/app/submission-history.test.mjs` con 7 tests (vacio inicial, add, orden cronologico inverso, tipos mixtos, maximo 20, clear, singleton). 108/108 tests pasan. lint, build, test pasan.
 - MVP-036: Agregada persistencia via `localStorage` a `submission-history.ts`. El historial ahora se guarda en `scot_submission_history` del navegador y sobrevive a recargas de pagina. `ensureHydrated()` carga desde storage al primer acceso; `persist()` guarda tras cada mutacion. Todo acceso a storage encapsulado en try/catch para entornos sin localStorage (SSR, Node.js). Tests ampliados con 4 casos de persistencia usando mock storage (guardar, limpiar, hidratar, recarga simulada). 112/112 tests pasan. lint, build, test pasan.
 - MVP-037: Agregado `removeHistoryEntry(index)` a `submission-history.ts` para eliminacion granular de entradas. Ambos formularios ahora muestran un boton "Limpiar" en la cabecera del historial que vacia todo el historial local y de localStorage. Tests ampliados con 3 casos de `removeHistoryEntry` (por indice, persiste eliminacion, out-of-bounds no-op). 115/115 tests pasan. lint, build, test pasan.
- MVP-037-CORR-001: Corregida hidratacion del historial en ambos formularios. `useState` ahora usa un inicializador perezoso que lee `getHistory()` al montar el componente, mostrando inmediatamente las entradas persistidas en localStorage sin esperar un nuevo submit. 115/115 tests pasan. lint, build, test pasan.
- Revisada MVP-037: lint/build/test pasan, pero requiere correccion importante. El historial persistido no se hidrata en el estado local de `InstructionForm` y `WasteForm` al montar o al volver a la vista, asi que lo guardado en `localStorage` no se ve hasta que ocurre un nuevo submit. La parte visible del historial queda a medias despues de recargar o cambiar de modulo.
- Generada MVP-037-CORR-001 para hidratar el historial local en los formularios al montar y al volver a la vista, de modo que lo persistido se vea realmente sin esperar un nuevo submit.
 - Aprobada MVP-036 completa: historial local persistente en navegador listo, resiliente en SSR/Node sin romper, y cobertura verde.
 - Generada MVP-037 para mejorar la visibilidad del historial con una accion de vaciado o control local mas claro desde la UI.
- Aprobada MVP-035 completa: historial local visible integrado en formularios, cobertura verde y sin tocar dominio, SQL, RLS ni Supabase real.
- Generada MVP-036 para persistir el historial local entre recargas con almacenamiento local, manteniendo el alcance sin backend real.
- Aprobada MVP-033 completa: resolver de perfil pluggable, API estable y cobertura verde; sin tocar formularios, dominio, SQL, RLS ni Supabase real.
- Aprobada MVP-034 completa: placeholder de autenticacion futura listo para inyectar sin tocar la UI ni la capa de submit.
- Generada MVP-035 para crear un historial local visible de subidas recientes en las formas, sin tocar backend real.
- Aprobada MVP-029: puente compartido de submit listo, estado configurable y cobertura verde; sin tocar UI, dominio, SQL, RLS ni Supabase real.
- Generada MVP-030 para conectar los formularios existentes al puente compartido de submit sin duplicar logica de persistencia.
- MVP-030: Modificados InstructionForm.tsx y WasteForm.tsx para usar submitInstructionFlow/submitWasteRecordFlow del puente compartido. Los formularios ahora validan localmente, pasan datos validados al submit flow, y muestran exito real (Instruccion/Merma registrada correctamente) o error de persistencia (en rojo). Agregado estado submitting con boton deshabilitado + texto "Enviando...". El submit flow auto-configura un no-op submitter por defecto (sin Supabase real ni configuracion explicita). Actualizado tests/app/operation-submit-flow.test.mjs: "throw when not configured" reemplazado por "auto-configure no-op and return ok=true". 86/86 tests pasan. Sin cambios en services/, domain/, supabase/, submit-flow layout, database/, SQL, RLS ni dependencias. lint, build, test pasan.
- MVP-038: Convertidas las paginas de indice `/instructions` y `/waste` de placeholders a vistas operativas completas de historial local. `HistoryClient.tsx` de ambos modulos ahora hidratan el historial persistido desde `getHistory()` al montar via inicializador perezoso de `useState`. Agregados: boton "Limpiar historial" (rojo, solo visible con entradas), boton individual "X" por entrada que llama `removeHistoryEntry` con busqueda por timestamp en el arreglo maestro, y llamada a `setEntries` post-mutacion para re-render inmediato. Muestran: titulo del modulo, boton "Nueva" al formulario, contador de entradas, estado vacio con texto de ayuda, y listado de entradas con timestamp `toLocaleString("es-MX")` y resumen. 115/115 tests pasan. lint, build, test pasan. Sin cambios en dominio, SQL, RLS, Supabase ni dependencias.
- MVP-039: Creada vista unificada de historial operativo en `/history`. `HistoryClient.tsx` hidrata todo el historial persistido (`getHistory()`) al montar. Muestra: resumen con conteos (total, instrucciones, merma), filtros de tipo (Todo/Instrucciones/Merma) con estado activo visual, acceso directo a `/instructions` y `/waste`, estado vacio, y listado de entradas con badge de tipo (azul para instruccion, ambar para merma), timestamp y resumen. Soporta eliminacion individual y limpieza total con re-render inmediato. Pagina principal (`/`) agrega tarjeta "Historial Operativo" (HO) de ancho completo (`sm:col-span-2`) que enlaza a `/history`. 115/115 tests pasan. lint, build, test pasan. Sin cambios en dominio, SQL, RLS, Supabase ni dependencias.
- MVP-040: Creado catalogo local de productos de prueba en `src/lib/app/product-catalog.ts` con 20 productos, busqueda por codigo de barras/nombre/categoria (substring case-insensitive), `getProductById` y `getProductByBarcode`. Creada pagina `/products` con `ProductsClient.tsx` que muestra el catalogo completo con busqueda en vivo y boton "Copiar ID" para portapapeles. Integrado en `WasteForm.tsx`: boton "Catalogo" al lado del campo product_id que despliega busqueda inline con resultados y boton "Usar" que rellena product_id y codigo de barras; el campo codigo de barras ademas auto-completa product_id cuando se ingresa un codigo existente. Agregada tarjeta "Catalogo de Productos" (CP) en pagina principal y enlace "Catalogo de productos" en pagina de merma. Creados `tests/app/product-catalog.test.mjs` con 16 tests. Script test actualizado. 131/131 tests pasan. lint, build, test pasan. Sin cambios en dominio, SQL, RLS, Supabase ni dependencias.
- Revisada MVP-040: lint, build y test pasan, pero requiere correccion importante en `src/app/waste/new/WasteForm.tsx`. Al escribir un codigo de barras conocido, el formulario autocompleta `product_id`; sin embargo, si luego el usuario cambia ese codigo por otro sin coincidencia, el `product_id` previo queda intacto. Esto puede enviar una merma con barcode y producto desalineados. Falta limpiar o recalcular `product_id` cuando el barcode deja de coincidir, y falta una prueba que cubra ese flujo.
- Generada MVP-040-CORR-001 para alinear `barcode` y `product_id` en el formulario de merma cuando se edita manualmente el codigo y deja de existir coincidencia en el catalogo local.
- MVP-040-CORR-001: Corregida la logica de `barcode` onChange en `WasteForm.tsx`. Cuando el valor no coincide con ningun producto del catalogo, `product_id` se limpia a `""`; cuando el valor esta vacio y `product_not_found` es falso, tambien se limpia. El autocompletado cuando hay coincidencia se conserva. Agregada prueba "transition from known to unknown barcode" en `tests/app/product-catalog.test.mjs`. 132/132 tests pasan. lint, build, test pasan. Sin cambios en dominio, SQL, RLS, Supabase ni dependencias.
- MVP-040-CORR-002: Agregadas 6 pruebas en `tests/app/product-catalog.test.mjs` bajo `waste-form barcode to product_id resolution` que reimplementan la logica pura del onChange de WasteForm como funcion `resolveOnBarcodeChange`. Cubren: barcode conocido asigna ID, cambio a barcode desconocido limpia ID, barcode vacio, product_not_found=true no autocompleta, transicion entre dos productos conocidos cambia ID correctamente, y que product_not_found=true inhibe cualquier autocompletado. WasteForm.tsx no fue modificado (la correccion funcional de CORR-001 se conserva). 138/138 tests pasan. lint, build, test pasan. Sin cambios en dominio, SQL, RLS, Supabase ni dependencias.
- Revisada MVP-040-CORR-002: la cobertura sigue sin quedar amarrada al codigo real del formulario. En `tests/app/product-catalog.test.mjs` se define una funcion local `resolveOnBarcodeChange(...)` que copia la logica del `onChange` de `WasteForm.tsx`, pero no la importa ni la ejecuta desde el archivo fuente. Eso significa que el test puede seguir pasando aunque la logica real del formulario cambie o se rompa. Falta extraer la resolucion de barcode/product_id a una funcion compartida del propio modulo permitido y probar esa funcion directamente.
- Generada MVP-040-CORR-003 para extraer y probar la logica real de resolucion `barcode -> product_id` desde `WasteForm.tsx`, sin tocar dominio, SQL ni Supabase.
- MVP-040-CORR-003: Extraida funcion `resolveProductIdFromBarcode` a `src/app/waste/new/barcode-resolution.ts` y conectada desde `WasteForm.tsx`. El test ahora importa la funcion real desde el helper `.ts` compartido, evitando la importacion directa de `.tsx` que rompe el runner de Node. La funcionalidad se mantiene: barcode conocido autocompleta, desconocido limpia, vacio limpia, `product_not_found=true` inhibe autocompletado. `npm test`, `npm run lint` y `npm run build` pasan. Sin cambios en dominio, SQL, RLS ni Supabase.
- MVP-041: Agregado escaner de codigo de barras con camara en formulario de merma. Creado `src/app/waste/new/barcode-scanner.ts` con funciones puras `isBarcodeDetectorSupported()`, `startCamera()`, `stopCamera()`. Creado `BarcodeScannerClient.tsx` como componente cliente que muestra preview de camara, usa `BarcodeDetector` API nativa para detectar codigos, y muestra fallback claro cuando el navegador no soporta el detector o la camara no esta disponible. Integrado en `WasteForm.tsx` con boton "Escanear" al lado del campo barcode; al detectar un codigo rellena el barcode y resuelve `product_id` via `resolveProductIdFromBarcode`. Creado `tests/app/barcode-scanner.test.mjs` con 1 test. El flujo manual de barcode y product_id se conserva intacto. `npm run lint`, `npm run build` pasan. 138/138 tests existentes + 1 nuevo pasan. Sin cambios en dominio, SQL, RLS, Supabase ni dependencias.
- MVP-042: Agregada seccion de evidencia de imagen opcional en formulario de merma. Creado `src/app/waste/new/evidence-attachment.ts` con `validateImageFile()` que verifica tipo MIME (JPEG, PNG, WebP, GIF, BMP, AVIF) y `readFileAsDataURL()` para preview local. `WasteForm.tsx` reemplaza el placeholder "Foto de evidencia: pendiente de implementar" con input de archivo oculto (`accept="image/*"`, `capture="environment"`) que permite seleccion desde dispositivo o camara; muestra preview en miniatura con boton "Quitar" y nombre del archivo. Error claro si el archivo no es imagen valida. El flujo de reset tambien limpia la evidencia. Creado `tests/app/evidence-attachment.test.mjs` con 10 tests de `validateImageFile`. `package.json` actualizado para incluir el nuevo test. `npm run lint`, `npm run build` pasan. 148/148 tests pasan (138 existentes + 10 nuevos). Sin cambios en dominio, SQL, RLS, Supabase ni dependencias.
- MVP-043: Convertida vista local de merma en bandeja operativa. `submission-history.ts` extendido con interfaz `WasteMeta` (reviewStatus, hasEvidence, evidenceName, barcode, quantity, unit, responsiblePerson, reason, area, observation), funcion `updateWasteReviewStatus(index, status)` para cambiar estado localmente, y `addHistoryEntry` ahora acepta meta opcional. `WasteForm.tsx` guarda meta estructurado al registrar incluyendo estado inicial `pendiente_revision` y datos de evidencia. `waste/HistoryClient.tsx` reescrita como bandeja: muestra resumen de conteos por estado, filtros por estado de revision, filtros por evidencia, tarjetas con metadatos (motivo, area, responsable, cantidad), badge de estado de revision con colores, indicador de foto, selector desplegable para cambiar estado localmente (persiste en localStorage via `updateWasteReviewStatus`). Eliminacion individual y limpieza total preservadas. Tests ampliados con 7 pruebas de meta y `updateWasteReviewStatus`. El historial de instrucciones no se modifica. `npm run lint`, `npm run build` pasan. 155/155 tests pasan (148 existentes + 7 nuevos). Sin cambios en dominio, SQL, RLS, Supabase ni dependencias.
- MVP-044: Convertida vista local de instrucciones en bandeja operativa. `submission-history.ts` extendido con `InstructionMeta` (status, priority, responsiblePerson, instructionText, observations), `INSTRUCTION_STATUSES_LOCAL`, y funcion `updateInstructionStatus(index, status)`. `InstructionForm.tsx` guarda meta estructurado al registrar con estado inicial `pendiente`. `instructions/HistoryClient.tsx` reescrita como bandeja: resume conteos por estado, filtros por estado y prioridad, tarjetas con badge de estado y prioridad con colores, metadatos (responsable, observaciones), selector desplegable de estado local que persiste en localStorage via `updateInstructionStatus`. Entradas legacy (sin meta persistido) se manejan via `updateInstructionStatus` que auto-crea meta completo. Eliminacion individual y limpieza total preservadas. Tests ampliados con 5 pruebas. El historial de merma sigue funcionando (read-only `waste/HistoryClient.tsx` intacto).
- MVP-044-CORR-001: Reemplazado tipo union `WasteMeta | InstructionMeta` por interfaz plana `HistoryEntryMeta` con todos los campos obligatorios para compatibilidad con componentes de solo lectura (`waste/HistoryClient.tsx` y `WasteForm.tsx`). Eliminado `HistoryMeta` type. Agregados helpers `createWasteMeta()` y `createInstructionMeta()` con valores por defecto. `addHistoryEntry()` ahora acepta `Partial<HistoryEntryMeta>` y completa todos los campos segun el tipo de entrada. `WasteMeta` conservado como interfaz independiente usada por `WasteForm.tsx` para su variable local. `updateWasteReviewStatus` y `updateInstructionStatus` auto-crean `HistoryEntryMeta` completo en entradas legacy. `instructions/HistoryClient.tsx` simplificado eliminando type guard `isInstructionMeta`. Build, TypeScript y 159/159 tests pasan sin modificar componentes de solo lectura.
- MVP-045: Vista `/history` mejorada como consulta operativa. Creado `src/app/history/history-filters.ts` con `filterHistory()` (filtro por tipo, texto, rango de fecha y estado), `getFilterStatuses()` y `statusLabel()`. `HistoryClient.tsx` reescrito con: busqueda por texto en vivo, filtros por tipo, estado y rango de fecha, detalle expandible por entrada que muestra metadata estructurada (codigo, cantidad, responsable, motivo, area, evidencia para merma; responsable, instruccion, prioridad, observaciones para instruccion), badge de estado coloreado visible siempre. Entradas sin metadata se muestran sin romper. Eliminacion individual y limpieza total preservadas. Creado `tests/app/history-filters.test.mjs` con 20 tests del helper puro. 179/179 tests pasan. `npm run lint`, `npm run build` pasan. Sin cambios en dominio, SQL, RLS, Supabase ni componentes de solo lectura.
- MVP-046: Agregado filtro por responsable en `/history`. `history-filters.ts` extendido: `FilterOptions.responsiblePersonFilter`, logica en `filterHistory()`, nueva funcion `getUniqueResponsiblePersons()`. `HistoryClient.tsx` agrega selector desplegable de responsable que convive con tipo/estado/texto/fecha. Filas de botones de filtro envueltas en `overflow-x-auto` para scroll horizontal en mobile. Filtro de responsable se actualiza al cambiar tipo. 8 nuevos tests de `filterHistory` con `responsiblePersonFilter` y `getUniqueResponsiblePersons`. 187/187 tests pasan. `npm run lint`, `npm run build` pasan. Sin cambios en dominio, SQL, RLS ni Supabase.
- MVP-048: Creada guia de uso minimo y arranque rapido en `docs/USO_MINIMO.md` con enfoque operativo de tienda (roles, cuando usar IO vs MR, barcode/catalogo, evidencia, foto opcional, que hacer si falta producto). Documenta que los asistentes no usan la app y deja recomendaciones para celular y computador. Actualizado `README.md` con resumen de arranque rapido, referencia a la guia y seccion de pruebas. Documento 100% ASCII. `npm run lint`, `npm run build` y `npm test` pasan. Sin cambios en codigo, dominio, SQL, RLS, Supabase ni dependencias.
- MVP-049: Creada guia de despliegue en Vercel en `docs/DEPLOY_VERCEL.md` con resumen del camino de despliegue, prerequisitos, variables de entorno (`NEXT_PUBLIC_SUPABASE_URL` y `NEXT_PUBLIC_SUPABASE_ANON_KEY`), pasos de despliegue, checklist de preproduccion, checklist de verificacion post-despliegue y seccion de fallos comunes (build, Supabase, escaner, evidencia, historial). Actualizado `README.md` con seccion "Despliegue en Vercel" y resumen del camino. Documento 100% ASCII. `npm run lint`, `npm run build` y `npm test` pasan. Sin cambios en codigo, dominio, SQL, RLS, Supabase ni dependencias.
- MVP-050: Agregada exportacion JSON local del historial filtrado desde `/history`. Creado `src/app/history/history-export.ts` con `buildExportPayload()`, `generateExportFilename()` y `triggerDownload()`. La exportacion incluye entradas visibles, filtros aplicados, marca temporal y conteo. `HistoryClient.tsx` agrega boton "Exportar JSON" en la barra de herramientas, deshabilitado cuando no hay registros filtrados. Creado `tests/app/history-export.test.mjs` con 12 tests del helper puro. 199/199 tests pasan. `npm run lint`, `npm run build` pasan. Sin cambios en dominio, SQL, RLS ni Supabase.
- MVP-051: Agregada importacion JSON local a `/history`. Creado `src/app/history/history-import.ts` con `validateImportPayload()` y `readImportFile()`. Agregada funcion `importHistoryEntries()` a `submission-history.ts` que valida, normaliza e importa entradas respetando MAX_ENTRIES. `HistoryClient.tsx` agrega boton "Importar JSON", input oculto de archivo `.json`, y banner de resultado (verde exito / rojo error). Creados `tests/app/history-import.test.mjs` con 12 tests de validacion y `tests/app/submission-history.test.mjs` con 10 tests de importacion. 221/221 tests pasan. `npm run lint`, `npm run build` pasan. Sin cambios en dominio, SQL, RLS ni Supabase.
- MVP-052: Agregado modo seguro de importacion (`combinar` / `reemplazar`) en `/history`. `submission-history.ts` exporta tipo `ImportMode` y `importHistoryEntries()` acepta modo opcional; `"replace"` llama a `clearHistory()` antes de importar. `history-import.ts` reexporta `ImportMode`. `HistoryClient.tsx` agrega selector de modo (botones "Combinar" / "Reemplazar") junto al boton Importar JSON, deshabilitado durante la importacion. Mensaje de exito incluye el modo usado. Tests ampliados con 4 casos de modo replace. N/A tests pasan. `npm run lint`, `npm run build` pasan. Sin cambios en dominio, SQL, RLS ni Supabase.
- MVP-053: La importacion JSON local detecta y omite duplicados exactos (mismo type, timestamp, summary y meta). `submission-history.ts` agrega interfaz `ImportSummary` con `imported`, `duplicates`, `discarded`; `importHistoryEntries()` ahora retorna `ImportSummary` en vez de `number`, usando `entryKey()` para comparacion. `history-import.ts` reexporta `ImportSummary`. `HistoryClient.tsx` muestra resumen claro: importados, duplicados omitidos y descartados. Tests actualizados para nuevo retorno y agregados 4 tests de dedup. 229/229 tests pasan. `npm run lint`, `npm run build` pasan. Sin cambios en dominio, SQL, RLS ni Supabase.
- MVP-054: Agregada accion de deshacer para eliminaciones y limpiezas del historial en `/history`. `submission-history.ts` guarda un snapshot profundo del arreglo `entries` antes de `removeHistoryEntry` y `clearHistory` via `saveUndoSnapshot()`. Nuevas exportaciones: `getUndoSnapshot()`, `undoHistory()` (restaura el snapshot y persiste), `clearUndoSnapshot()`. `HistoryClient.tsx` muestra barra "Entrada eliminada." o "Historial limpiado." con boton "Deshacer" y opcion de descartar. 10 nuevos tests de undo cubren: sin snapshot, snapshot tras remove/clear, restauracion, snapshot nulo post-undo, solo ultima accion, out-of-bounds no guarda, clear vacio no guarda. 239/239 tests pasan. `npm run lint`, `npm run build` pasan. Sin cambios en dominio, SQL, RLS ni Supabase.
- MVP-055: El snapshot de deshacer del historial ahora se persiste en localStorage (claves `scot_undo_snapshot` y `scot_undo_message`) y sobrevive a recargas accidentales de pagina. `submission-history.ts` agrega `persistUndoMessage()`, `getUndoMessage()` y `loadUndoSnapshotFromStorage()`; `saveUndoSnapshot()` escribe tambien a localStorage; `getUndoSnapshot()` y `undoHistory()` cargan desde storage si la copia en memoria es null; `clearUndoSnapshot()` limpia ambas claves. `HistoryClient.tsx` inicializa `undoMessage` desde `getUndoMessage()` al montar y persiste el mensaje via `persistUndoMessage()` en cada clear/remove. 7 nuevos tests de persistencia de undo. 246/246 tests pasan. `npm run lint`, `npm run build` pasan. Sin cambios en dominio, SQL, RLS ni Supabase.
- MVP-056: El snapshot de deshacer ahora expira a los 5 minutos. `submission-history.ts` agrega constante `UNDO_EXPIRY_MS = 5 * 60 * 1000`, variable `undoSnapshotCreatedAt`, y envuelve los datos persistidos en `{ createdAt, snapshot }`. `getUndoSnapshot()` y `undoHistory()` verifican expiracion tanto en memoria como al cargar desde storage; si expiro, limpian el snapshot y retornan null/false. 3 nuevos tests de expiracion. 249/249 tests pasan. `npm run lint`, `npm run build` pasan. Sin cambios en dominio, SQL, RLS ni Supabase.
- MVP-057: Persistido y restaurado el estado de filtros de `/history` en localStorage (clave `scot_history_filters`) para que la vista vuelva como la dejo el usuario tras recargar la pagina. Creado `src/app/history/history-ui-state.ts` con `saveFilterState()` y `loadFilterState()` con validacion de tipos y fallback a valores default. `HistoryClient.tsx` ahora inicializa `filters` desde `loadFilterState()` al montar y persiste via `useEffect` en cada cambio. Creados `tests/app/history-ui-state.test.mjs` con 13 tests (carga, guardado, errores, round-trip, storage no disponible, valores invalidos). 262/262 tests pasan. `npm run lint`, `npm run build` pasan. Sin cambios en dominio, SQL, RLS ni Supabase.
- MVP-058: Persistida y restaurada la entrada expandida de `/history` en localStorage (clave `scot_history_expanded`) para que el detalle que el usuario dejo abierto se recuerde al recargar la pagina. `history-ui-state.ts` agrega `saveExpandedState()` y `loadExpandedState()` con almacenamiento directo del timestamp como string numerico y eliminacion de clave al colapsar. `HistoryClient.tsx` inicializa `expanded` desde `loadExpandedState()` y persiste via `useEffect` en cada cambio. Tests ampliados con 11 nuevos casos (guardado, carga, valores no numericos, round-trip, colapso, storage no disponible). 273/273 tests pasan. `npm run lint`, `npm run build` pasan. Sin cambios en dominio, SQL, RLS ni Supabase.
- MVP-059: Persistido y restaurado el modo de importacion (`Combinar`/`Reemplazar`) de `/history` en localStorage (clave `scot_history_import_mode`) para recordar la seleccion del usuario tras recargar la pagina. `history-ui-state.ts` agrega `saveImportMode()` y `loadImportMode()` con validacion del valor y fallback a `"combine"`. `HistoryClient.tsx` inicializa `importMode` desde `loadImportMode()` y persiste via `useEffect` en cada cambio. Tests ampliados con 9 nuevos casos (carga con combine/replace/unknown, guardado, round-trip, storage no disponible). 282/282 tests pasan. `npm run lint`, `npm run build` pasan. Sin cambios en dominio, SQL, RLS ni Supabase.

## 2026-06-05

- MVP-061: Conectado el modulo de Instrucciones Operativas al backend Supabase con fallback local. Creado `src/lib/app/instructions-repository.ts` con `fetchInstructions()`, `updateInstructionStatus()`, `removeInstruction()` y `clearInstructions()` que eligen automaticamente entre Supabase (SELECT/UPDATE) y localStorage (via submission-history) segun `getRuntimeMode()`. Creado `src/app/instructions/useInstructions.ts` como hook React con estados loading/error/refresh. Creado `src/app/instructions/InstructionsClient.tsx` que reemplaza el HistoryClient anterior, con filtros por estado y prioridad, selector de estado que persiste localmente o en base de datos, y soporte de modo local para eliminar/limpiar. Creados 3 archivos de test con 22 nuevos casos: `instructions-repository.test.mjs` (9 tests de fetch/update/remove/clear en local), `instructions-flow.test.mjs` (5 tests de ciclo create-fetch-update), `instructions-ui.test.mjs` (6 tests de logica de filtros). `InstructionForm.tsx` modificado para omitir `addHistoryEntry` en modo supabase (la data ya vive en Supabase). `page.tsx` de `/instructions/new` actualizado con texto descriptivo de modo dual. 312/312 tests pasan. `npm run lint`, `npm run build` pasan. Sin cambios en dominio, SQL, RLS ni modificacion de archivos prohibidos.

- MVP-062: Conectado el modulo de Merma al backend Supabase con fallback local. Creado `src/lib/app/waste-repository.ts` con `fetchWasteRecords()`, `updateWasteReviewStatusRepo()`, `removeWasteRecord()`, `clearWasteRecords()` y helpers de evidencia local (`saveEvidenceLocal()`, `getEvidenceLocal()`, `removeEvidenceLocal()`). Creado `src/app/waste/useWasteRecords.ts` como hook React con loading/error/refresh. Creado `src/app/waste/WasteClient.tsx` que reemplaza WasteHistoryClient, con filtros por estado de revision y presencia de evidencia, selector de estado que persiste localmente o en Supabase, visualizacion de evidencia local, y soporte de modo local para eliminar/limpiar. Creados 3 archivos de test con 24 nuevos casos: `waste-repository.test.mjs` (12 tests de fetch/update/remove/clear/evidence), `waste-flow.test.mjs` (6 tests de ciclo create-fetch-update + evidence round-trip), `waste-ui.test.mjs` (6 tests de logica de filtros). `WasteForm.tsx` modificado para omitir `addHistoryEntry` en modo supabase y persistir evidencia local via `saveEvidenceLocal()`. `page.tsx` de `/waste/new` actualizado con texto descriptivo de modo dual. 336/336 tests pasan. `npm run lint`, `npm run build` pasan. Sin cambios en dominio, SQL, RLS ni archivos prohibidos.
- MVP-063: Cargado el catalogo real de productos desde el Excel compartido (`EAN`), normalizado a `src/lib/app/product-catalog-data.json` con 9,624 registros. `src/lib/app/product-catalog.ts` ahora lee ese archivo y expone busqueda por EAN, material y nombre, conservando UUID internos para compatibilidad con `product_id`. `ProductsClient.tsx` muestra la base completa y `WasteForm.tsx` usa el catalogo para resolver producto por EAN/material y mostrar el material code + descripcion en el resumen. Se corrigio el import JSON con atributo `with { type: "json" }` para compatibilidad con Node/Next. 334/334 tests del bloque actualizado pasan y el build/lint permanecen limpios. Sin cambios en SQL, RLS ni Supabase.
- MVP-064: Convertido el modulo de productos a busqueda contra repositorio sin cargar todo el catalogo en memoria. `product-repository.ts` ahora importa `product-catalog.ts` mediante dynamic `import()` en vez de static import, rompiendo la cadena de bundling de las 9,624 entradas (1.6 MB JSON) en el cliente. `ProductsClient.tsx` eliminó `fetchAllProducts()` al montar y ahora solo llama `searchProducts(query)` con debounce de 300 ms, mostrando estados de busqueda, error y resultados sin cargar el catalogo completo. `WasteForm.tsx` cambió sus imports de `product-catalog.ts` a `product-repository.ts`, unificando todo el acceso a productos a través del repositorio dual (local/Supabase). `resetProductRepo()` ahora también limpia la promesa cacheada del repositorio local para correcta isolación en tests. `searchProducts` de `product-repository.ts` en modo local retorna todas las coincidencias (la UI limita despliegue a 120). Sin cambios en dominio, SQL, RLS ni Supabase. 352/352 tests pasan.
- MVP-065: Creada utilidad de seed/import del catalogo real de productos (9,624 entradas) hacia Supabase, idempotente y con modo dry-run. Creado `src/lib/supabase/product-seed.ts` con `createSupabaseProductSeed(supabase)` que implementa `seedProducts(products, { dryRun? })`: consulta codigos de barras existentes en Supabase, salta duplicados e inserta en lotes de 100 los productos nuevos, mapeando `materialCode → category`. Creado `src/lib/app/product-seed.ts` con `seedProductsToSupabase(dryRun?)` que verifica el modo de ejecución via `getRuntimeMode()`, retorna error descriptivo si Supabase no está configurado, o delega en el adaptador Supabase vía dynamic `import()`. Creados `tests/app/product-seed.test.mjs` con 8 tests (dry-run sin datos, dry-run con existentes, insercion completa, salto de duplicados, error de select, error de insert, modo local sin configurar, dry-run flag preservado). `product-repository.ts` re-exporta `ProductCatalogEntry` para compatibilidad de tipos. Sin cambios en dominio, SQL, RLS, migraciones ni UI. 360/360 tests pasan. `npm run lint` (0 errores), `npm run build` pasan.
- MVP-066: Conectado almacenamiento de evidencia de merma a Supabase Storage con fallback local. Creado `src/lib/supabase/waste-storage.ts` con `createWasteStorage(supabase)` que expone `uploadEvidence()`, `getPublicUrl()` y `deleteEvidence()`. Extendido `src/lib/app/waste-repository.ts` con `uploadWasteEvidence()` (subida dual local/Supabase), `updateWasteEvidencePath()` (actualiza columna `evidence_path` en Supabase), `getLatestWasteRecordId()` y `getEvidenceDisplayUrl()` (resuelve URL pública desde storage path). `WasteForm.tsx` ahora sube la evidencia a Supabase Storage después del envío exitoso en modo supabase y enlaza el `evidence_path`. `WasteClient.tsx` resuelve URLs de evidencia de Supabase Storage asincrónicamente al hacer clic en "Ver evidencia". Creados `tests/app/waste-storage.test.mjs` con 14 tests (upload, extensión, error de upload, getPublicUrl, deleteEvidence, upload dual, getEvidenceDisplayUrl local, updateWasteEvidencePath local, getLatestWasteRecordId local). Sin cambios en dominio, SQL, RLS ni migraciones. 374/374 tests pasan. `npm run lint`, `npm run build` pasan.
- MVP-067: Conectada autenticacion real de Supabase Auth con login/logout, resolucion de perfil y manejo de sesion. Creado `src/lib/supabase/auth.ts` con `createSupabaseAuth(supabase)` que expone `login()`, `logout()` y `getSession()`. Reescribito `src/lib/app/auth-profile.ts`: `createAuthProfileResolver()` retorna resolver sincrono que devuelve `""` (sesion no resuelta) en vez de lanzar error; nueva funcion `tryResolveAuthProfile()` que obtiene la sesion de Supabase via `auth.getSession()`, cachea el `user.id` como perfil activo y actualiza el estado de autenticacion. Agregado `getAuthStatus()`/`setAuthStatus()` a `current-profile.ts` con estado `"loading" | "authenticated" | "unauthenticated"`, y `resetProfileResolver()` limpia el estado. `app-bootstrap.ts` llama `tryResolveAuthProfile()` al final del bootstrap en modo supabase. Creado `src/app/login/LoginClient.tsx` con formulario de email+password, boton de cerrar sesion y deteccion de modo local. Creado `src/app/login/page.tsx` como wrapper. `layout.tsx` muestra indicador de sesion ("Verificando...", "Sin sesion", "Sesion: usuario_id...") con enlace a login/cuenta. Creados `tests/app/auth.test.mjs` con 6 tests del adaptador Supabase Auth (login, error login, logout, error logout, session con sesion, session sin sesion). `auth-profile.test.mjs` actualizado para nuevo comportamiento (resolver retorna `""`, tryResolveAuthProfile sin sesion retorna `""` y marca unauthenticated). `current-profile.test.mjs` ampliado con 4 tests de getAuthStatus/setAuthStatus. Sin cambios en dominio, SQL, RLS, migraciones ni rutas existentes. 387/387 tests pasan. `npm run lint` (0 errores), `npm run build` pasan.
- MVP-068: Creada capa de resolucion auth -> perfil interno de tienda con nombre, rol y estado. Creado `src/lib/supabase/profile-repository.ts` con `createSupabaseProfileRepo(supabase)` que expone `fetchProfileByUserId()` consultando la tabla `profiles`. Creado `src/lib/app/profile-repository.ts` con `fetchProfileByUserId()` dual-mode: en modo supabase delega en el adaptador (lazy import), en modo local retorna el perfil de desarrollo. Agregado `DEV_PROFILE` en `dev-profile.ts` con `display_name: "Usuario de Desarrollo"`, `role: "supervisor"`, `status: "activo"`. `current-profile.ts` extiende con `getCachedProfile()`, `setCachedProfile()`, `getCurrentProfileDisplayName()`, `getCurrentProfileRole()` y variable `cachedProfile`. `auth-profile.ts` ahora llama `fetchProfileByUserId(userId)` tras autenticar y guarda el perfil via `setCachedProfile()`; en caso de fallo o perfil faltante, `cachedProfile` queda `null`. `app-bootstrap.ts` en modo local configura `setCachedProfile(DEV_PROFILE)`. `layout.tsx` muestra `display_name (rol)` en vez de solo `user.id`; si no hay perfil, muestra "Sesion iniciada — sin perfil de tienda". `LoginClient.tsx` muestra nombre, email y rol del perfil autenticado. Creados `tests/app/profile-repository.test.mjs` con 5 tests (select con user_id, perfil nulo, error DB, local mode, supabase mode sin env vars). Sin cambios en dominio, SQL, RLS ni migraciones. 392/392 tests pasan. `npm run lint` (0 errores), `npm run build` pasan.
- MVP-069: QA integral end-to-end (MVP-069) sobre la aplicacion conectada a Supabase. Corregidos estados locales de historial para alinearse con los estados reales del dominio (`cumplida`, `no_cumplida`, `anulada`, `recuperable`, `no_recuperable`, `requiere_seguimiento`, `anulado`) en `submission-history.ts`, `history-filters.ts`, `HistoryClient.tsx` (principal), `waste/HistoryClient.tsx` e `instructions/HistoryClient.tsx`. Actualizados tests de `history-filters` y `submission-history` para reflejar los estados correctos (7 opciones por tipo). Corregido `BarcodeScannerClient.tsx` para evitar re-montes infinitos usando `useRef` en `onDetect`. Corregido `ProductsClient.tsx` para limpiar `setTimeout` al desmontar y dar feedback visual al copiar ID al portapapeles. Eliminado import no usado `configureProductRepo` en `tests/app/products-ui.test.mjs` para quitar warning de lint. `npm run lint` (0 errores), `npm run build` y `npm test` (392/392) pasan. Sin cambios en dominio, SQL, RLS, Supabase ni migraciones.
- MVP-070: Validacion final de cierre de ciclo y estabilizacion previa a prueba real de tienda. Revisados flujos completos: login, home, productos, instrucciones, merma, historial y evidencia. Corregido `WasteForm.tsx`: el timestamp de evidencia local ahora se toma del `HistoryEntry` recien creado via `getHistory()[0]`, eliminando el desfase de milisegundos entre el `Date.now()` capturado antes de `addHistoryEntry` y el timestamp real del entry. Esto repara la evidencia fotografica en modo local (bandeja de merma ahora puede recuperar la foto adjunta). Corregido `LoginClient.tsx`: `router.push()` reemplazado por `window.location.href` tras login y logout para forzar recarga de pagina y que el header de `layout.tsx` refresque correctamente el estado de autenticacion. Corregido `WasteClient.tsx`: el boton "Ver evidencia" ahora solo abre el preview cuando la URL de evidencia existe, evitando imagen rota cuando el archivo local no esta disponible. Guias `USO_MINIMO.md` y `DEPLOY_VERCEL.md` verificadas y alineadas con el comportamiento actual. Modo local verificado: la app arranca sin variables de entorno, con DEV_PROFILE y submitter de desarrollo. `npm run lint` (0 errores), `npm run build` y `npm test` (392/392) pasan. Sin cambios en dominio, SQL, RLS, Supabase ni migraciones.
 - MVP-071: Cierre de release y preparacion de despliegue en Vercel. Convertido `layout.tsx` de componente cliente a servidor para habilitar metadata estatica en HTML. Creado `src/app/ClientLayout.tsx` como componente cliente que encapsula el bootstrap, header de autenticacion y badge de modo. Agregado `metadata` export a todas las rutas: `/`, `/instructions`, `/instructions/new`, `/waste`, `/waste/new`, `/products`, `/history`, `/login`. Esto permite que Vercel genere `<title>` y `<meta description>` en el HTML estatico para SEO, previews sociales y pestanas del navegador. Verificada coherencia entre local, preview y production: el modo local arranca sin env vars, modo supabase requiere `NEXT_PUBLIC_SUPABASE_URL` y `NEXT_PUBLIC_SUPABASE_ANON_KEY`. Build genera 10 paginas estaticas correctamente. `npm run lint` (0 errores), `npm run build` y `npm test` (392/392) pasan. Sin cambios en dominio, SQL, RLS, Supabase ni migraciones.
 - MVP-072: Piloto funcional y monitoreo post-despliegue - corregidas fricciones de UX real identificadas en simulacion de uso de tienda. Corregido `InstructionForm.tsx` y `WasteForm.tsx`: los formularios ahora se limpian automaticamente tras un envio exitoso, manteniendo el resumen del registro (lastValid) para confirmacion visual; el campo de evidencia tambien se limpia tras exito en merma. Agregada auto-focus en el primer campo (`responsable` en instrucciones, `codigo de barras` en merma) tras pulsar "Limpiar" para acelerar el registro continuo en un entorno de tienda. Agregadas confirmaciones `window.confirm()` antes de todas las acciones destructivas: limpiar historial en `InstructionsClient.tsx`, `WasteClient.tsx` y `HistoryClient.tsx`; eliminar entrada individual en `InstructionsClient.tsx`, `WasteClient.tsx` y `HistoryClient.tsx`; limpiar historial inline en `InstructionForm.tsx` y `WasteForm.tsx`. Esto previene perdida accidental de datos en un entorno movil con interacciones rapidas. `npm run lint` (0 errores), `npm run build` y `npm test` (392/392) pasan. Sin cambios en dominio, SQL, RLS, Supabase ni migraciones.
 - MVP-073: Consolidacion del soporte de arranque para tienda - protocolo de incidencias y documentacion de uso diario. Creado `docs/SOPORTE.md` con protocolo completo de soporte: niveles de atencion (tienda / tecnico / escalamiento), tipos de incidencia con primera respuesta (login, modulo, formulario, catalogo, evidencia, historial, app no carga), datos obligatorios para reportar, tabla de severidad con tiempos de respuesta, workarounds para seguir operando, y canales de soporte por completar. Actualizado `docs/USO_MINIMO.md`: reemplazada la seccion 10 "Si algo no funciona" por una seccion 10 "Solucion de problemas rapidos" con pasos accionables por tipo de fallo (app no carga, formulario no envia, historial vacio, escaner roto, evidencia no adjunta) y referencias cruzadas al protocolo de soporte. Actualizado `docs/DEPLOY_VERCEL.md`: agregada seccion 9 "Soporte post-despliegue para tienda" con resumen de entregables al equipo tecnico y referencias a SOPORTE.md y USO_MINIMO.md. Actualizado `README.md`: agregada referencia a `docs/SOPORTE.md` junto a la referencia existente de `docs/USO_MINIMO.md`. `npm run lint` (0 errores), `npm run build` y `npm test` (392/392) pasan. Sin cambios en codigo, dominio, SQL, RLS, Supabase ni migraciones.
 - MVP-074: Paquete de entrega final para adopcion real - cierre documental y operativo del MVP. Creado `docs/ENTREGA_FINAL.md` con: resumen ejecutivo de modulos y estado de entrega, checklist de arranque para tienda (infraestructura, dispositivos, documentacion, pruebas de humo), estado detallado de cada modulo (IO, MR, HO, CP, auth) con funcionalidades y limitaciones, tabla de 7 incidencias conocidas con impacto, workaround y plan de accion, referencias rapidas a los 6 documentos del proyecto, y checklist de validacion de salida. Actualizado `README.md`: agregada referencia a `docs/ENTREGA_FINAL.md` junto a las referencias existentes de USO_MINIMO.md y SOPORTE.md. `npm run lint` (0 errores), `npm run build` y `npm test` (392/392) pasan. Sin cambios en codigo, dominio, SQL, RLS, Supabase ni migraciones.
 - MVP-075: Handoff final de mantenimiento y cierre del proyecto. Creado `docs/HANDOFF.md` con: estado final del proyecto (lo que esta listo y lo que no esta en el MVP), arquitectura a grandes rasgos (stack, modos de ejecucion, estructura de directorios, flujo de datos), procedimientos de mantenimiento habitual (agregar productos, crear usuarios, cambiar roles, rotar claves, actualizar dependencias), limitaciones conocidas con mitigaciones y plan futuro, punto de partida para futuras ampliaciones ordenadas por complejidad (mejoras UX, nuevas funcionalidades, ampliaciones de arquitectura), patron de 10 pasos para agregar un nuevo modulo, tabla de documentacion del proyecto, versiones y compatibilidad, y cierre formal del MVP. Actualizado `README.md`: agregada referencia a `docs/HANDOFF.md` junto a las referencias existentes. `npm run lint` (0 errores), `npm run build` y `npm test` (392/392) pasan. Sin cambios en codigo, dominio, SQL, RLS, Supabase ni migraciones.
